{
  "name": "vivi-lang",
  "private": true,
  "workspaces": [
    "packages/compiler",
    "packages/docs"
  ],
  "scripts": {
    "dev:compiler": "npm run dev --workspace packages/compiler",
    "dev:docs": "npm run dev --workspace packages/docs",
    "test": "npm run test -workspace packages/compiler",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "tsup": "^8.1.0",
    "typescript": "^5.4.5"
  }
}
