{
  "name": "vivi-lang",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:compiler": "yarn dev --workspace packages/compiler",
    "dev:docs": "yarn dev --workspace packages/docs",
    "test": "yarn test -workspace packages/compiler",
    "build": "yarn workspaces run build"
  },
  "devDependencies": {
    "tsup": "^8.1.0",
    "typescript": "^5.4.5"
  },
  "resolutions": {
    "string-width": "4.2.3"
  }
}