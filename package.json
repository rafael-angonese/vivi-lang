{
  "name": "vivi-lang",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:compiler": "yarn workspace @fantastic/compiler dev",
    "dev:docs": "yarn workspace docs dev",
    "test": "yarn workspaces run test",
    "build": "yarn workspaces run build"
  },
  "devDependencies": {
    "tsup": "^8.1.0",
    "typescript": "^5.4.5"
  },
  "resolutions": {
    "string-width": "4.2.3"
  }
}